# markitdown-mcp

Converts documents (PDF, DOCX, XLSX, PPTX, HTML, TXT, MD) to Markdown. Stateless — no database, no storage.

## Tools

- **process_document**: Convert a local file to Markdown. Returns `filename`, `markdown` content, and `markdown_size_bytes`.
